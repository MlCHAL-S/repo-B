name: CI

on:
  workflow_dispatch:   # allows manual or API triggers
  repository_dispatch:   # trigger from Repo A
    types: [run-ci]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Repo B workflow triggered!"
